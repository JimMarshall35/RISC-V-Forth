
{
    "version" : "0.2.0",
    "configurations" : 
    [
        {
            "name" : "debug risc-v",
            "type" : "cppdbg",
            "request" : "launch",
            "program" : "${workspaceFolder}/QEMUForth.elf",
            "cwd" : "${workspaceFolder}",
            "miDebuggerPath" : "riscv-none-elf-gdb",
            "miDebuggerServerAddress" : "localhost:1234",
            "stopAtEntry" : true,
            "preLaunchTask" : "Run QEMU"
        }

    ]
}